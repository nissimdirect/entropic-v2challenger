"""Q7 L-backbone worker process — STUB for PR #4.

This package will host the real multi-headed L inference worker in PR #9
(Session 2). PR #4 ships the IPC shape + the entry-point + ZMQ ping/encode
stub so the topology decision (DEC-Q7-008) has working scaffolding.

Run via:
    python3 -m q7_worker --port 6099
"""

__version__ = "0.0.1"
