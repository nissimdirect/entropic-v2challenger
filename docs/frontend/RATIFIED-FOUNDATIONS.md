# RATIFIED FOUNDATIONS — Creatrix Frontend
**Ratified 2026-07-29 by nissimdirect, F-1 walkthrough (live app + B3 mockup + frame mock side by side).**
**This file is the canonical copy (landed via F1). New load-bearing decisions append a row here in the same PR that implements them — never retroactively.**
Standing rule: specs trace to rows in this table; new load-bearing decisions get a row at the moment they're made, never retroactively.
Exception: rows may be back-filled for PRE-EXISTING banked decisions discovered to lack rows (coverage repairs, marked as such) — the never-retroactively rule applies to NEW decisions.

| # | Decision | Verdict | Detail |
|---|----------|---------|--------|
| D1 | "Ableton for video effects" × Photoshop direction | ✅ RATIFIED | Market frame + mental model for every future feature. |
| D2 | Track = Layer; arrangement IS the layers panel; row order = z-order | ✅ RATIFIED | Permanent law: no feature may ever add a separate layers panel. |
| D3 | Lean track headers + right-dock selection-driven LAYER panel | ✅ RATIFIED | Deep controls (blend/opacity/matte/transform) live in the panel, never the header. |
| D4 | Icons: wire-restyle set (wand-as-Block exception) + locked glyphs (razor R2c, ripple D5) + grouped rail Convention 1 (8 groups, cycle hotkeys) | ✅ RATIFIED | Wand W1/W2/W3 small-size pick remains OPEN (comet variants in progress, parallel session). |
| D4a | Grouped rail group count | 🔧 **AMENDED 2026-07-30 (orchestrator ruling, veto-by-exception open)** | The 07-15 "8 groups" verdict was made against the invalid 1600px mock's imagined 14+3 toolset. Real inventory = 14 tools → **6 groups** (SELECT V · TRIM B · MASK-SHAPE Q · MASK-FREE W · KEY E · MARK/LOOP **Shift+M** — bare `m` stays add_marker per F-0516-8). TEXT/NAV groups ship with their future tools, no placeholder slots (REAL-INVENTORY-ONLY). **PARTIALLY SUPERSEDED-PENDING by D10/R1 (2026-07-31):** grouping/cycling/hotkeys stand; the rail's POSITION and subtool-interaction model (flyout vs drawer) rule at the W2 step-0 reconciliation — see RECONCILIATIONS.md R1. |
| D5 | Mono-only identity: JetBrains Mono everywhere; hierarchy via weight/size/color; Plex two-voice plan formally dead | ✅ RATIFIED | Canon (DESIGN-SPEC.md) must record the Plex reversal. |
| D6 | Type scale | 🔧 **AMENDED → SCALE B+1** | User: "make text a bit bigger than min" → bump everything +1: **heading 16/650 · body 14/450 · label 13/600 · data 12/450. App floor = 12px** (was 11). Supersedes "Scale B locked 2026-07-10". Final values confirmed by eye on a REAL-DIMENSIONS mock during PK.A. **Downstream: PK.A token values change; type-floor ratchet target = 12px (recount violations: all <12px); ui-foundation proposal.md/design-spec.md need an amendment commit on PR #447.** |
| D7 | Empty states: minimal styled hint text, no CTA buttons | 🔧 **RATIFIED w/ CONTRAST GUARANTEE** | User: "concerned about contrast." Hint text must meet WCAG AA (4.5:1) on its surface — bind to a token pairing verified in DESIGN-SPEC §9's computed table, and PK.D's UAT row gains a screenshot+PIL contrast oracle. Quiet ≠ illegible. |
| D8 | Automation strip grouping | 🔧 **AMENDED — curve ops leave the strip** | User: "curve ops i think are not actual buttons i think its more like in the lane." Strip keeps **Mode (R/L/T/D)** and **Record (Overdub/+Lane/+Trigger/+Mod)** clusters + wrap-safe layout. **Flatten/Ramp/Shape/Simplify/Clear move to lane-contextual actions** (on-lane affordance or lane context menu — action lives at the object it acts on, which also kills the "which lane?" ambiguity of a global button). Exact interaction decided at PK.C's real-dims mock review. **Downstream: PK.C re-scoped; OD-6 superseded.** **Affordance: A — lane right-click context menu (user pick, 2026-07-30).** PK.C build note: Simplify/Clear/Shape… relocated cleanly (their store actions already take an explicit laneId). Flatten/Ramp STOP — **resolved by team-lead adjudication, Option A refined:** `flattenSelectedPoints`/`rampSelectedPoints` have no laneId parameter (selection-driven only), so both appear in the lane CURVE menu but render **disabled** (BEM `--disabled` + `aria-disabled`) unless the clicked lane already owns a qualifying same-lane point selection (Flatten ≥1 point, Ramp ≥2); enabled, they fire the existing handler unchanged — zero new algorithms, standard menu grammar (Photoshop/Premiere disable inapplicable items). |
| D9 | Build order: ui-foundation before every queued feature change | ✅ RATIFIED | Framework phases wrap around it, never ahead of it. |
| D10 | Unified rail — Rail v12 (banked ㊺/㊻, FINAL) | ✅ RATIFIED (row added 2026-07-31 — ratification-coverage gap: the decision lived in the browser-folders lane) | ONE permanent leftmost rail: containers top (INSTRUMENTS ⌸ · EFFECTS ⌁ · GENERATORS ◍ · OPERATORS ∿ · UTILITIES ⚒ · PRESETS ▤ · USER LIBRARY ⌂, category-colored) · tools bottom (mono, radio-select) · per-icon exclusive-accordion drawer (header glyph·name·count·«, « = only close) · ONE search above all, expand-on-type · tool drawers = live options, 📌 pin · corner dot = has-drawer. Implemented by browser-folders (v4.1 W2) after a RECONCILIATIONS row vs the built Convention-1 rail. **Does not displace D4/D4a's cycling contract; positional conflicts resolve via R1.** |
| D11 | Translucency convention | ✅ RATIFIED (user, 2026-07-31) | "See-through … usually indicate[s] a selection." Opacity = identity; translucency = transient state (selection/drag/preview). Master track loses its wash identity (v4.1 W1-6); convention recorded in COMPONENT-SPEC §2¾. |
| D12 | Selection/loop grammar (W1.5b OD-2) | ✅ RATIFIED (owner, 2026-07-31 — "2 - sounds good") + ✅ BUILT (PK.A4, orchestrator ruling, 2026-07-31, PR #488) | Locks proposal.md OD-2's recommended default: click-drag on any lane bed (empty or not, any track incl. master) creates a time-range **selection**, snapped to the grid when quantize is on, free when off; `Cmd+L` copies the current selection → `loopRegion` (Ableton grammar); the loop brace stays independently draggable/settable via existing `loop_in`/`loop_out`. The modifier-split question this row originally left open (Alt+drag proposed, unconfirmed) is now RESOLVED: the orchestrator ruled UNIFICATION, not a split — `MarqueeOverlay.tsx`'s existing rubber-band clip-select drag was already a horizontal-only, time-range gesture in effect (full lane height always implicitly selected), so one plain drag now produces both outputs from a single commit — snapped `selectionRegion` (live during the drag) plus RAW-pixel-range clip selection (unchanged predicate) — with `Alt` held during the drag providing a temporary, per-drag snap bypass instead of gating a second gesture. Implemented inside `MarqueeOverlay.tsx`; no competing handler. |

## New rule ratified in passing
**REAL-DIMENSIONS-ONLY** (sibling of REAL-INVENTORY-ONLY): mocks are authored inside the app's default window (1280×800) and sanity-checked at minimum supported size. Enforced by a fixed-shell-width grep in the mock-registry verification script. Trigger: user caught `ui-foundation-frame.html` hard-coded at 1600px (25% wider than the app's default window), plus a malformed media query — width-sensitive judgments made on it (rail collisions, strip wrapping) must be re-checked at 1280×800 before PK.B/PK.C build. Pixel-type-size judgments survive.

## Provenance (the user's own words behind D1–D5, for the record)
"Ableton mixed with Photoshop" · "the second" · "each track IS a layer" · "oof this is worse" (B2) · "the actual tracks in the arrangement view are the layers" · "too much info to squeeze into a track header — maybe a panel that links to it on the right; just not the layer order" · "definitely block is best" → wire dictations (07-15/18) · "i legit cant use anything like this" (07-09).

