# RECONCILIATIONS — cross-lane coherence ledger (A4)

> When two planning lanes name the same surface, the overlap gets a row HERE before either
> builds. Guard: FRONTEND-SDLC pipeline step 7½. Origin: Convention-1 rail × Rail v12
> collided undetected across lanes (2026-07-31).

| # | Surfaces | Lanes | Ruling | Status |
|---|---|---|---|---|
| R1 | The left tool strip (ToolRail.tsx ×  Rail v12 tool zone) | ui-foundation (Convention-1 grouped rail, built #464) × browser-folders (banked ㊺/㊻) | TRANSFERS: glyphs, cycle-hotkey dispatch, ARIA machinery, tests. CHANGES: strip position/chrome (merges into unified rail bottom zone). OPEN RULING (owner, by eye at W2 mock): subtool groups as press-hold flyouts (as built) vs v12 tool-drawers. Cite-refresh of browser-folders file:line anchors required pre-dispatch (App.tsx/EffectBrowser lines rotted under F3/F4). | OPEN — rules at W2 step-0 |
| R2 | App.tsx (shared file, disjoint regions) | w15b-grid-track-paradigm (PK.C1: transport-bar JSX only, App.tsx:3498-3577) × browser-folders (sidebar render switch ~3637-3691 + preview-row rail mount ~3756) × multiwindow-stage-a × system-monitor-v1 × util-transform × wave0-prerouted-presets (each claims its own App.tsx region — window plumbing / status cluster / transform mount / preset apply respectively) | w15b's claim is the transport-bar region ONLY — disjoint from every other lane's App.tsx region. No design conflict with w15b. ORDERING: whichever lane merges second re-verifies line anchors in the others' packet docs (mechanical refresh, no ruling). NOTE: pairings among the OTHER five lanes are NOT resolved by this row — they predate w15b and stay in the guard's baseline-18 ledger until each pair reconciles. | CLOSED for w15b pairings only |
| R3 | stores/timeline.ts (shared store, additive fields) | w15b-grid-track-paradigm (PK.A4: adds selectionRegion + setters; PK.B2: no store change, subscriber only) × layertap-matte-v1 (Lane 4: matte/routing fields on track/clip records) | Both additive, disjoint fields — no shared-field conflict. ORDERING: second merger rebases and re-runs both changes' store tests; if layertap-matte-v1 adds an undo-tracked field while w15b's selectionRegion is undo-transparent, keep the distinction explicit in undo.ts wiring. | CLOSED — ordering note only |
