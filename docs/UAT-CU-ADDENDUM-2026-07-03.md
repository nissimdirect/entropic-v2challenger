# CU-UAT ADDENDUM — PR-Traced Rows for the 30-Day Audit — 2026-07-03

**Companion to** `docs/UAT-PLAN-2026-07-02-live-cu.md` (its runtime protocol applies verbatim: canonical checkout launch + live-runtime path check, throwaway projects for anything destructive, screenshot-per-verdict, ✅❌🐛⏸ only, effect-amount-nonzero before any "render broken" claim) **and** `docs/UAT-PR-TRACEABILITY-2026-07-03.md` (the audit these rows close).
**Why this doc exists:** UAT was passing things that weren't there. Every row here is traced to a PR; the executor MUST read that PR's claims in the traceability doc BEFORE validating, and the oracle names the falsifiable on-screen/pixel evidence. The **Trap** column states the exact rubber-stamp this row is designed to catch.
**Hard rules inherited:** temporal/stateful effects → verdict only during multi-frame Play (single-frame preview shows nothing — learning #44) · alpha/matte claims → export + PIL pixel assertion, never preview eyeballs (JPEG drops alpha) · GAP rows (⚠ marked) test code-verified gaps and are EXPECTED TO FAIL today — a pass means the gap was fixed; a rubber-stamped ✅ without the oracle evidence is itself a finding.

**Contents:** 82 CU rows across 32 stages + 14 backend-only command oracles. 73 source items (16 code-verified gaps ⚠ + 57 unmapped features).


## Stage B (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X230-1 ⚠GAP | #230 | Setup (shell): with the app closed, create/edit a project JSON in a throwaway project directory to set a sampler instrument's loop config to {enabled:true, in:0, out:30, dir:'pingpong', crossfade:8} (SamplerDevice.tsx exposes no in-app loop UI — grep confirms `grep -n loop frontend/src/renderer/components/instruments/SamplerDevice.tsx` returns nothing, so hand-edit is the only entry point). Drive: launch the app fresh, File->Open the throwaway project, select the MIDI track with the sampler, press Play and scrub the playhead back and forth across the loop boundary (frame 30) repeatedly, watching the preview. | Playback across the loop boundary visibly reverses direction (ping-pong) confirming fwd/rev/pingpong direction WIRED, screenshot each direction change. AT THE SEAM (frame 30 boundary crossing) the frame POPS/jumps abruptly rather than cross-fading smoothly — confirms crossfade is a no-op (GAP_MISSING, export.py:1601 has zero call sites outside its own test). | Only confirming the loop plays back and direction changes (which is real and would pass), then assuming crossfade smoothing also works because 'the loop feature works' — must explicitly zoom on the seam frame-by-frame to catch the missing blend. |


## Stage C (10 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X273-2 | #273 ▶multi-frame | Setup: create a throwaway project. Add a field-capable effect to a clip (e.g. Color Invert or any effect exposing a field-bindable numeric param — check Inspector > Effect state for a 'Field' badge/icon next to a param slider). Assign an image as a 2D field source per Stage C5 flow (Inspector Track shows the lane). Drive: scrub the playhead across several frames while the field-driven param is visible in the Inspector param readout; then export a short MP4 (File > Export, H.264, default settings) covering those frames. | screenshot of the Inspector param value changing per-pixel-field-driven output across at least 3 scrubbed frames, AND the exported MP4's decoded frame (via ffmpeg extract + PIL) visually matches the corresponding preview frame (non-crash, non-blank) — confirms field_codegen dispatch didn't silently no-op or crash on this Mac's GPU/CPU path | accepting a static/unchanged preview as 'field applied' — per-pixel field effects only show correctly across frames with the field's spatial variation visible; a flat/uniform output means the pointwise dispatch guard in pipeline.py silently fell through |
| X280-1 | #280 | Open Routing Canvas via hotkey Cmd+Shift+I (label 'Routing Canvas' per default-shortcuts.ts). Create two Kentaro/operator nodes A and B. Drag-connect A -> B, then drag-connect B -> A to attempt to close a 2-node cycle. | Screenshot showing either (a) the second drag-connect is rejected outright (no new edge line drawn, cursor/drop shows a blocked state), or (b) the canvas displays the banner text 'cycle detected in routing — N node(s) involved' (RoutingCanvas.tsx:471) identifying which edge was auto-broken | PR claims a `cycle_safe_edge_addition` pre-check exists but PARTIAL status shows no confirmed caller in RoutingCanvas or modulation engine code — if neither the block nor the banner appears (i.e. the cyclic graph is silently accepted and rendered), that confirms the pre-check is unwired dead code, not a false pass |
| X283-1 | #283 | With the A->B->A cycle from X280-1 still present in Routing Canvas (Cmd+Shift+I), press Play on the transport and let it render several frames, then check the cycle banner text again and note which specific edge (A->B or B->A) is shown as removed — repeat by deleting and recreating the same two nodes/edges in the SAME order 3 times. | the removed edge is IDENTICAL across all 3 repeats (deterministic lex-smallest-edge break, not declaration-order or random) — screenshot of the banner's node-id list each time, diffed for consistency | an old declaration-order fallback would still 'work' by removing edges consistently in the same session but non-deterministically across different node creation orders — this row must recreate nodes with SWAPPED creation order (create B before A) and verify the SAME lex-smallest edge (not creation-order-first edge) is broken, proving it's truly lex-based not order-based |
| X285-1 | #285 | With a modulation cycle present (from X280-1), open Export dialog (the 'Export' menu/button per ExportDialog.tsx), choose H.264 (MP4) tab, click the export action button, and watch for toast/notification during the export progress (App.tsx:1162 fires a toast with message=cycleWarning exactly once via sg5CycleWarnSeenRef). | exactly ONE toast/notification appears during the whole export (not one per frame — watch the full progress bar to completion, count toast appearances), and its text matches the routing canvas's cycle description; exported MP4 plays back identically to what live preview showed for the same frame range | a per-frame-spam toast (defeats the 'once-per-export' claim) or a MISSING toast (cycle_warning field never populated) both look superficially like 'export succeeded' if you don't watch the whole progress bar — must count toast occurrences, not just check export completed |
| X295-1 | #295 ▶multi-frame | Add a Granulator instrument to a track (Instruments Browser > Granulator per InstrumentsBrowser.tsx). Push density near/at MAX_GRAINS (right-click the density knob per GranulatorDevice.tsx:273 or drag it to max) with ~200 grains active. Press Play and observe preview responsiveness for 5+ seconds, then export a short MP4 covering the same range (File > Export, H.264). | preview plays without a visible stutter/latency spike (screenshot of consistent frame timing, or FPS counter if visible in statusbar) at high density — GPU path engaged; then decode 2-3 frames from the exported MP4 with ffmpeg + PIL and pixel-compare against the corresponding LIVE PREVIEW screenshot at the same timeline position — must match within a small tolerance (confirms CPU-export path matches GPU-preview path, no drift) | judging 'GPU grain-render works' from smooth preview alone — the PR's real risk is preview/export DRIFT (different code path at export vs preview per granulator_gpu.py `is_export` coercion); skipping the pixel-diff step and eyeballing the exported video only would rubber-stamp a silent divergence between GPU-preview and CPU-export composites |
| X296-1 | #296 | Add a Granulator to a track and right-click a knob (e.g. density, per GranulatorDevice.tsx:273 onContextMenu=instrumentLearnContextMenu) to arm MIDI Learn (per instrumentLearn.ts comment: 'right-clicking a knob arms a slot LearnTarget'). If no hardware MIDI controller is connected, use a virtual MIDI source sending rapid CC messages (e.g. via a virtual MIDI utility) at >30 msgs/sec for 2 seconds on the same CC. | the mapped Granulator parameter's on-screen value updates smoothly (no visible jank/lag/stutter in the knob position) despite the input flood — confirms the 33ms-floor rate limiter in midi.ts is capping writes; screenshot of the parameter value settling to the final CC value without runaway oscillation | the echo-suppression sub-claim (recordEmit within 80ms window) is PARTIAL/unconfirmed in code — if a motorized-fader or feedback loop causes visible VALUE OSCILLATION (parameter flickering between two values), that specifically falsifies the echo-suppression claim even if the rate-limiter itself works fine; don't conflate the two sub-claims in one pass/fail |
| X297-1 | #297 | In Routing Canvas (Cmd+Shift+I) or Modulation Matrix, set up a runtime-conditional modulation edge (an operator mapping whose bindingRule depends on live playback state, e.g. an audio-follower or video-analyzer edge per OperatorRack). Corrupt/malform its runtime context if a UI path exists to do so (e.g. bind it to a deleted/missing source track so evaluating it at runtime raises); otherwise hand-edit the saved project's mapping to reference a nonexistent operator id for that edge type, then reload the project and press Play. | timeline continues playing without a crash or frozen frame (screenshot mid-playback showing playhead advancing, no error dialog interrupting playback); a SINGLE console/log line noting the degradation appears once (check DevTools console or `~/.creatrix/logs/sidecar.log`), not repeated once per frame during the whole playback | if the app crashes or the timeline freezes on the malformed edge, that's the exact regression this fix targets — do not accept a frozen preview as 'graceful' just because no dialog popped up; also confirm the degradation log doesn't spam (scroll the full console log across the playback duration, not just the first line) |
| X300-1 | #300 | Setup: build any project with an operator mapping (Operator Rack, add an LFO/Envelope operator with a mapping bound to a clip/effect param), save it, then quit the app. Hand-edit the saved .glitch project file's JSON on disk (`grep -n 'mapping' ~/.creatrix/projects/<file>.glitch` to locate it) and set that mapping's `min` field to `NaN` (or `max` to `Infinity`). Relaunch the app and use File > Open Project to load the hand-edited file, then also attempt File > Export on it. | the app surfaces a visible error (dialog or toast) containing text matching 'must be a finite number' (security.py:773-778 wording) either at load or at export-attempt time, and the project does NOT silently load/render with the NaN/Infinity value in effect — screenshot of the error surface plus confirmation the corrupted mapping is not applied to the render | the validator (security.py validate_operator_mod_edges) is wired at the RENDER/EXPORT boundary and per-frame IPC, not necessarily at plain project-file load (schema.py's separate _validate_operator_mod_edges only checks bindingRule/axis/depth, NOT min/max) — if the file opens silently with no error and only FAILS (or silently clamps) once you press Play or Export, that is still a PASS for this row, but if it silently renders using the NaN/Infinity value with no error ever surfacing, that FAILS the claim; test both load and render/export to pin down exactly where (if anywhere) the guard fires |
| X418-1 | #418 | Throwaway project. Add fx.copy_machine ('Copy Machine' in the Effect Browser search box) to a clip's effect chain (drag onto the track/clip or use the Effect Rack '+' add). Right-click the track header to open its context menu and confirm the automation submenu now lists 'Add Trigger: Copy Machine > Rewind' (a bool param — it must appear ONLY under an 'Add Trigger:' entry, never an 'Add Lane:' entry). Click it to create the trigger lane. Left-click once on an empty region of the new trigger lane at ~2s to place a pulse (click toggles 0/1 at the clicked time per Track lane click behavior), then click again ~1s later to close the pulse. Press Play from before the pulse and let playback run through and past the pulse region (multiple frames, per stateful-effect rule). | Screenshot the track context menu showing the literal label 'Add Trigger: Copy Machine > Rewind' with no sibling 'Add Lane: Copy Machine > Rewind' entry. During playback, capture 3 frames: pre-pulse, mid-pulse, post-pulse — the mid-pulse frame must show the copy_machine output visibly playing BACKWARD (content order reversed vs. pre-pulse frame; e.g. compare frame content to the timeline's earlier frames to confirm reversed direction), and the post-pulse frame must resume forward motion (ring re-fills). Precheck: with Rewind never triggered, confirm the copy_machine output changes at all across frames (effect-amount-nonzero) before judging the pulse's effect. | Rubber-stamp risk: eyeballing a single paused frame during the pulse and calling it done — a static frame cannot show 'reversed' motion, only multi-frame comparison across the pulse window proves the ring actually rewound instead of just freezing or glitching once. |
| X418-2 | #418 | Same throwaway project/clip. Open the clip's Effect Rack param panel for Copy Machine and locate the 'Auto-Reverse At' knob (reverse_at, range 0-120, default 0=off). Set it to a low nonzero value (e.g. 5). Alternatively use the AutomationToolbar 'Add Automation Lane' picker (continuous, non-trigger mode) and confirm 'Copy Machine > Auto-Reverse At' is selectable there (it is a float, so it must appear in the continuous 'Add Automation Lane' picker, NOT filtered into trigger-only). Let the effect run generations up until it should cross the threshold (drive 'generation' via whatever param feeds it, e.g. repeated processing over time via Play), and observe. | Screenshot confirming 'Auto-Reverse At' appears under the 'Add Automation Lane' (continuous) picker list, not only the 'Add Trigger Lane' list. During playback, capture frames before and after generation crosses the set threshold: the output ring must auto-fire a rewind (visible reversed playback) without any manual trigger click, then re-arm (return to forward) once generation drops back below threshold — confirm via a second manual generation-threshold crossing producing a second auto-rewind. | Rubber-stamp risk: testing only that the knob is draggable and has a number readout, without ever crossing the threshold during live playback — that would pass a param that is fully unwired to the runtime auto_fire logic (claim is specifically about the auto-fire *mechanism*, not the knob's existence). |


## Stage F (5 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X219-1 ⚠GAP | #219 | Setup: New Project -> import a clip -> select the clip on the timeline. Drive: press q on the preview canvas to enter marquee-rect mode, drag a rectangle over the video -> commits as a rect MatteNode. Press Delete (delete_selected) -> observe delete-inside consumption. Undo, draw the marquee again, press Alt+Backspace (mask_delete_outside) -> observe delete-outside consumption. | Preview frame after Delete shows the marqueed region replaced by transparency/checkerboard (delete-inside) via screenshot; after Alt+Backspace the OUTSIDE region is gone instead. Both PASS (already confirmed live per docs/UAT-RESULTS-2026-06-17.md). | Confirming only marquee-creation and skipping the delete-inside/outside consumption step, missing a regression in setClipMaskMode wiring. |
| X219-2 ⚠GAP | #219 | Setup: same clip with a committed rect MatteNode from X219-1 (redo the marquee if needed). Drive: search the UI for ANY affordance to set the matte's mode to 'fill' with a swatch color — check the MaskStackPanel (opens automatically when a clip with matte nodes is selected, shows per-node rows with Enable/Move-up/Move-down/Delete icons per MaskStackPanel.tsx) and the device-chain mask-routing dropdown ('device-mask' row on DeviceCard) for a 'Fill' option or color swatch picker. | Screenshot of MaskStackPanel node row and device-mask dropdown showing NO fill-mode toggle or swatch-color control exists (only Enable/Move up/Move down/Delete are present per MaskStackPanel.tsx:137-166). Confirms setClipMaskMode('fill', maskFillColor) has zero UI call site — GAP reproduces. | Concluding 'masking works' from the delete-inside/outside pass in X219-1 without separately hunting for the fill-mode control, silently passing an UNWIRED claim. |
| X227-1 ⚠GAP | #227 | Setup: New Project -> import a clip -> press q for marquee, drag a small ellipse (hold to cycle to marquee-ellipse if needed) -> deliberately trigger a wand-sample IPC failure: switch to wand tool (toolMode='wand', two preview-overlay icons bottom-left of preview) and click a coordinate while the asset path is not yet ready (e.g. click immediately after import before the .asset-badge / ingest finishes, or click outside canvas bounds if reachable) to force the mask_wand_sample call to fail. | A toast appears reading 'Wand sample failed — try again' (source wand-sample-failure per MaskSelectOverlay.tsx:285-293) instead of the wand click silently doing nothing. | Only testing the success path (wand click that works) and never forcing failure, so the toast-on-failure claim goes unverified. |
| X254-1 | #254 | Setup: New Project -> import a clip -> select clip -> drag an effect onto DEVICE CHAIN -> press q, draw a rect marquee, press Delete to commit a matte and set effect.maskRef via the device-mask dropdown on the DeviceCard (assign the rect matte to the effect). Drive: observe the DeviceCard's mask chip area (masking__matte-chip, 64x36px, title 'Masked: node <id>'). | The chip renders a real grayscale <img data-testid='device-matte-chip-img'> (screenshot at high zoom showing an actual rasterized rect shape, not text) instead of the fallback 'MSK'/'INV' text badge (DeviceCard.tsx:504-508). | Glancing at the chip from a normal screenshot distance and assuming any small dark rectangle is 'a thumbnail' — must zoom in to confirm it's a rasterized image element (data-testid device-matte-chip-img) and not the text badge span rendered small. |
| X254-2 | #254 | Setup: same clip/effect from X254-1. Drive: change the effect's maskRef to a procedural key type (chroma/luma/color/AI key node instead of rect/ellipse) via the device-mask dropdown, and observe the DeviceCard mask chip again. | Chip falls back to the plain text badge reading 'MSK' or 'INV' (no <img data-testid='device-matte-chip-img'> present), and no error/crash toast appears — confirms the null/procedural-kind fallback path per PR claim. | Only testing the static-matte success path (X254-1) and never testing the procedural-kind fallback, missing a crash if _handle_mask_thumbnail mishandles procedural node kinds. |


## Stage J (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X231-1 | #231 | Setup: New Project -> add MIDI track -> drag a sampler from INSTRUMENTS tab onto it -> load a footage sample. Add an LFO operator (Operators panel) and open the Modulation Matrix. Drive: in the matrix column headers, find the synthetic target labeled with the sampler's name and 'Scrub' (paramKey `sampler.<id>.scrub` per ModulationMatrix.tsx:39,109-111); click the LFO row x Scrub column cell to create a route. Press Play on the transport. | During playback, the sampler's footage frame visibly sweeps back and forth across its playable range in sync with the LFO's rate (screenshot 3 timestamps showing different frames at same relative LFO phase); export a short clip and confirm frame content differs across the export timeline in the same sweeping pattern. | Mapping the LFO to Scrub but pressing Play for under one LFO cycle, seeing no visible change, and false-failing — must play across at least 2 full LFO periods (temporal effect, learning #44). |


## NEW-a11y-hints (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X206-1 | #206 | Drive: (1) with no video loaded, look at the PreviewCanvas placeholder text 'No video loaded' (className preview-canvas__placeholder); (2) open the Effects panel with an empty effect chain to see 'No effects. Add from browser.' (effect-rack__placeholder); (3) open Preferences and find the 'Light — Coming soon' hint (preferences__hint); (4) open an empty Timeline to see its empty-state hint (timeline__empty-hint). | Screenshot each of the 4 hint strings against their dark background; each must read as clearly legible mid-gray text (--cx-text-3), NOT the washed-out near-invisible disabled gray (--cx-text-disabled) it was before. A useful falsifiable check: take a screenshot and sample the pixel color of the hint text glyphs — luminance should be visibly higher than a known --cx-text-disabled control still present elsewhere in the same view (e.g. a genuinely disabled button) for contrast comparison. | Without a side-by-side contrast comparison against a still-disabled element, a low-effort check might just say 'text is visible' since it's never literally 0% opacity — the point of this PR is a WCAG AA contrast bump, which requires comparing against the disabled baseline, not just confirming presence. |


## NEW-composite-terminal (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X190-1 | #190 | Setup: new project, import one video clip onto a new video track (no composite effect yet — track header should show no blend/opacity chip). Drive: click the '+ Composite' button in the track header (Track.tsx className 'track-header__add-composite', title 'Add a Composite effect to control this track's opacity and blend mode'). Then drag the opacity slider (track-header__opacity, aria-label 'Track opacity: N%') to 50% and change the blend-mode dropdown (title 'Blend mode') to a non-Normal mode. Press Cmd+Z once. | After clicking '+ Composite': the opacity/blend chip (e.g. 'Normal 100%') becomes visible and the slider/dropdown become interactive (no longer showing the 'Add a Composite effect to set track opacity' info toast on interaction). After the single Cmd+Z: the chip and controls disappear again and the track returns to its no-composite state in ONE undo step (not two) — confirming addEffect wrapped composite creation in a single undo transaction. | A shallow pass would just confirm 'a composite got added' without checking the ONE-undo-step claim — if creation actually took 2 undo steps (e.g. add effect + set default params as separate transactions), Cmd+Z would leave an orphan composite effect with default values, which is the exact bug this PR claims to prevent. |


## NEW-effect-browser-style (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X201-1 | #201 | Drive: open the Effects browser panel and look at the tab bar (data-testid='browser-tab-bar', 5 buttons data-testid='browser-tab-fx'/'browser-tab-op'/'browser-tab-composite'/'browser-tab-tool'/'browser-tab-instruments'). Click through each tab. Then scroll to the '+ Add Text Track' button (effect-browser__action-btn, text literally '+ Add Text Track'). | Screenshot the tab bar: the active tab must render as a solid ACID-colored filled segment (not a dashed/bordered debug box), inactive tabs show a visible hover-highlight when moused over, and keyboard-focusing a tab (Tab key) shows a visible focus ring. The '+ Add Text Track' button must render with a solid border (--cx-line-2) and solid background (--cx-surface-3) — explicitly NOT a dashed outline. | A pass here just eyeballing 'tabs look fine' without focusing each tab via keyboard would miss a focus-visible-ring regression, and without literally comparing dashed-vs-solid on the Add Text Track button (an easy visual to miss at a glance) the row would rubber-stamp the exact debug-styling leftover this PR was meant to remove. |


## NEW-effects-field-params (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X272-1 ⚠GAP | #272 | Throwaway project. Add an effect to a clip that DeviceCard shows a 'Field…' option for (fieldParamSet populated from effectInfo.fieldParams). Open the param row's dropdown and select 'Field…', then attempt to assign an image/video as the field source and check whether the gain slider, 'Invert field' button, and 'Clear field' button appear and are functional. | If the field-capable list is still empty at the time of this run (PR body's own admission: list_effects returned empty fieldParams for all effects at merge, only fixed by a separate follow-up), the 'Field…' option will not appear on ANY param at all -- screenshot every param row on at least 3 different effect types and confirm whether 'Field…' shows up anywhere. Report PASS only if a follow-up PR (post-#272) already closed this gap; otherwise this row must FAIL today as designed. | Agent tests one effect, doesn't see 'Field…', and just says 'feature missing' without checking whether the SEPARATE follow-up PR (that populates FIELD_TOP25/registry.field_capable) has landed yet -- the row must explicitly check current registry state, not assume #272 alone ships a working control. |


## NEW-export-replay (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X203-1 | #203 | Setup: throwaway project, add a Performance/MIDI track with a sampler/pad instrument loaded, arm it (track-header__auto-btn, 'Arm for automation recording'), record a short (~10s) multi-voice pad performance during playback (press multiple pads in sequence/overlap while Play is running). Drive: File > Export (or the Export dialog) to an image sequence twice in a row without changing anything, to two different output folders. | Run `shasum -a 256` (or equivalent) over every corresponding frame file pair from the two export runs — every pair must match exactly (byte-identical). Also visually spot-check 3-4 frames spanning the recorded performance to confirm every played note/voice appears rendered (no dropped voice) versus the live preview at the same timeline position. | This is NOT CU-visually-verifiable for 'byte-identical' — a shallow pass would just glance at two exports and say 'they look the same,' which misses subtle non-determinism (e.g. voice ordering, floating rounding) that only shows up in a pixel-exact hash diff; the row requires the actual shasum comparison as the oracle, not eyeballing. |


## NEW-generator (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X146-1 | #146 | Throwaway project. Import a video clip, add 'Grid Moire' from the Effects tab generator list with default params. Press Play and let the preview run 3-5 seconds observing continuously (per rule: temporal effect, single frame proves nothing). While playing, in the effect's parameter panel drag the 'A Rotate' slider and separately the 'B Rotate' slider (or the liquify param for mesh A vs B) to different non-zero values one at a time. | During playback the interference beat pattern (moiré fringes) is visibly non-black and shifts/animates continuously across frames (not a static image) — screenshot 3 frames ~1s apart and confirm the fringe pattern position differs between them. When only 'A Rotate' is changed, mesh A's grating visibly reorients while mesh B's grating orientation is unchanged between two screenshots — proving the two meshes are independent, not a single shared mesh. | A CU agent that takes one screenshot and declares 'moire pattern visible' without comparing multiple timestamped frames would rubber-stamp a static/frozen render as passing a claim explicitly about live animation and mesh independence (learning #44). |


## NEW-generator-gap (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X123-1 ⚠GAP | #123 | Throwaway project. Import any video clip to the timeline. Open sidebar tab 'Effects' (EffectBrowser). In the generator category list find 'Grid Moire' and click '+ Add Grid Moire' (drag-to-add or the 'Add {name}' button per EffectBrowser.tsx) to add fx.grid_moire to the chain with default params (A Size=12, A Angle=0, B Size=13, B Angle=6). Screenshot the preview. | Screenshot the preview frame and sample pixel luminance (or PIL histogram if exported): the current PR-146 fix claims a brightness-preserving blend keeps fringes visible; if instead the frame reads near-black (mean luma < ~0.10 on a 0-1 scale) this row FAILS confirming the gap PR #123 described. Report actual mean luma. | Rubber-stamping 'effect added, preview changed a little' without measuring luma would hide a still-present near-black collapse regression; this row must quote the numeric brightness result, not a vibe check. |


## NEW-hardware-mapping (3 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X351-1 ⚠GAP | #351 | Throwaway project. Connect/simulate a MIDImix (or any class-compliant MIDI controller). Click the 'MAP' button in the statusbar (data-testid=statusbar-map-toggle) to open MIDI Map mode. Turn/send one physical CC to bind it to a parameter on the currently focused effect (per the picker UI, data-testid=midi-map-picker). Close overlay. Now, WITHOUT using any UI, try to find a way to apply the Akai MIDImix factory CC-map preset (search Effects/Instrument/Settings menus and the MIDI Map overlay header for a 'Load factory' style action -- data-testid=midi-map-load-factory is present in the overlay per source, so click it and observe). | Clicking the factory-map control (if visibly enabled) should populate all 32 MIDImix CC slots in the midi-map-grid; if instead nothing happens, the button is disabled/absent, or no menu path exists to reach applyControllerProfile, that is the FAIL this row is designed to surface -- screenshot the grid state before/after plus a note of which UI path (if any) was found. | This is a known GAP (controllerProfiles.ts factory map has no live UI call site per audit doc E18/#31) -- a CU agent that just says 'I clicked something and it looked fine' without checking the actual CC grid values populated is rubber-stamping a broken feature. |
| X351-2 ⚠GAP | #351 | With MIDI Map mode open and one CC already bound to a target on the currently-focused effect (per X351-1 setup), select a DIFFERENT clip or track in the timeline to change focus (watch the MappingContextChip text change). Send the SAME physical CC number again and observe the midi-map-grid slot highlight / the parameter that visibly changes value in the effect panel. | The CC must now visibly drive a parameter belonging to the NEWLY focused context (grid highlight moves to a different slot, and turning the knob changes a different effect's parameter value on screen), with no residual effect on the previous context's parameter -- screenshot both before/after focus switch showing different bound targets for the same CC. | Focus-follows is claimed WIRED via applyBankModulations in App.tsx, but if the CC still drives the OLD context's parameter after switching focus, that is a stale-binding regression the row must catch rather than assume 'it's wired in code so it must work.' |
| X376-1 | #376 | Throwaway project with a MIDI controller connected and >=1 CC already bound in MIDI Map mode (setup as in X351-1). In the statusbar, locate the Bank HUD control next to the MAP chip (data-testid=statusbar-bank-hud, showing 'Bank 1/MAX' with left/right arrow buttons data-testid=statusbar-bank-hud-left/-right). Click the right arrow (aria-label='Page bank right'). Then send the same physical CC and open MIDI Map overlay to see which slot/target lights up. | HUD label must increment from 'Bank 1/MAX' to 'Bank 2/MAX' (screenshot before/after click), the right-arrow button must become disabled once MAX_BANK_PAGES-1 is reached (atEnd), and the left-arrow must be disabled at Bank 1 (atStart) with data-bank-index attribute matching. Turning the same CC after paging must resolve to a DIFFERENT bound target/slot highlight in the midi-map-grid than before paging. | HUD renders null when ccBankBindings is empty -- a tester who skips the CC-binding setup step will see no HUD at all and could wrongly report it as broken UI rather than the documented empty-state hide. |


## NEW-hotkey-discoverability (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X216-1 | #216 | Setup: New Project -> Cmd+I import any clip -> drag an effect from left-dock EFFECTS tab onto DEVICE CHAIN (bottom rack). Drive: right-click the effect card in the device chain to open its context menu; read the 5 rows 'Freeze up to here (N effects)', 'Unfreeze' (only if frozen), 'Flatten to file…', 'Save as Preset…', 'Save Chain as Preset…' — note the shortcut text shown to the right of each label. Then open Preferences -> Shortcuts (or the app's shortcuts panel) and find the bound keys for freeze_up_to / unfreeze_effects / flatten_to_video / save_effect_preset / save_chain_preset. | Screenshot of the context menu showing shortcut text next to each of the 5 labels; screenshot of Preferences->Shortcuts panel showing the SAME key combo for each of the 5 actions (byte-for-byte match, e.g. both show 'Cmd+Shift+F' for Flatten). Mismatch or blank-vs-bound = fail. | CU agent eyeballs the menu once and rubber-stamps 'menu items exist' without cross-checking against Preferences->Shortcuts, missing a stale/wrong keycap regression. |


## NEW-hover-help (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X199-1 | #199 | Setup: open the Inspector panel with a clip/effect selected so at least one control has hover help registered (any control under Inspector.tsx, found via [data-help-id] per Inspector.tsx:106). Drive: (a) move the mouse over that control and hold still for ~500ms without clicking; (b) then move the mouse into the help panel itself (className cx-inspector-hover-help) before it would time out; (c) press Escape while hovering; (d) Tab-focus the same control via keyboard (no mouse) and compare the help text shown. | (a) after ~400-500ms (300ms settle per PLAN §3.10) the hover-help body (data-testid='inspector-hover-help') populates with non-empty text matching the hovered control. (b) moving into the help panel itself keeps the text visible (sticky 400ms window survives entering the panel). (c) pressing Escape immediately clears/hides the help body. (d) keyboard-focusing the same control (no mouse) shows the IDENTICAL help text as step (a) — screenshot both and diff the text. | A rubber-stamp check would only confirm 'a tooltip appears on hover' and skip the keyboard-focus parity claim — if focusin wasn't actually wired to the same help-id lookup, keyboard-only users (the actual WCAG 1.4.13 concern) would get nothing, and that gap is invisible unless you explicitly test with Tab instead of the mouse. |


## NEW-instruments (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X155-1 | #155 | Import a video clip onto a video track. Click the timeline's '+ MIDI Track' button (Timeline.tsx:199) to create a Performance track and select it. Click the sidebar 'Instruments' tab (App.tsx sidebar-tabs__btn, label 'Instruments'). Double-click the 'Sampler' entry in the RACKS list (InstrumentsBrowser.tsx) to add it to the selected Performance track. In the device-chain area at the bottom (SamplerDevice, data-testid='sampler-device'), set 'Source' dropdown to the imported clip, then drag 'Speed' input to a non-default value (e.g. 2.0), 'Opacity' slider to ~0.5, and 'Blend' dropdown to 'multiply'. | Screenshot the preview before and after each control change: Opacity change visibly reduces the sampler layer's visual strength in the composite, Blend='multiply' visibly darkens vs 'normal', and playback speed audibly/visually differs (frame advance rate change) after Speed=2.0 vs 1.0 — compare two screenshots at the same timeline playhead position across a Play run. Then right-click the Performance track and select 'Delete Track' (Track.tsx:164 removeTrack) and confirm the sampler layer disappears from the preview on the next rendered frame. | Toggling a slider and taking only one screenshot (no before/after pair) would rubber-stamp a control that's visually wired to nothing — Opacity/Blend/Speed must each show a measurable before vs after difference, and track deletion must be confirmed to actually clear the composited layer, not just remove the UI row. |


## NEW-operators (3 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X264-1 | #264 | Open a track with an effect already added to its device chain. In the Operator Rack panel, click the collapsible header row '▶ Topology' (aria-expanded toggle) to expand it -- label flips to '▼ Topology' and the xyflow graph mounts. Drag an LFO operator (from Operator Rack or browser) onto an effect parameter to create a mapping. Observe the topology graph. | Screenshot 1: graph renders an edge between the LFO operator node and the mapped effect node, with the edge's stroke width/opacity visibly pulsing (rAF-driven animation) across 2-3 screenshots taken ~1s apart. Screenshot 2: click '▼ Topology' again to collapse -- graph DOM node (OperatorTopologyGraph root) must be absent, verified via not appearing in the next screenshot region where it was. | Agent takes one screenshot and calls the static edge 'confirmed' without capturing a second frame a moment later to prove the stroke width is actually animating rather than a frozen SVG path. |
| X267-1 | #267 | Open the Effect Browser panel, switch to/open the operator tab showing 'op-tab-content' grouped list (MODULATION / INPUTS / GATING headers with entries like 'LFO', 'Kentaro Cluster', 'Sidechain', 'Gate'). Drag the 'Kentaro Cluster' entry from the browser and drop it directly onto a device param knob on an existing DeviceCard (not the track header). | After drop: (1) OperatorRack shows a new Kentaro Cluster operator entry (addOperator called), (2) the param knob shows an active mapping indicator, (3) press Play and take 3+ screenshots across consecutive playback moments -- the param value/preview must visibly modulate frame-to-frame (per PR's own 28/30-frame Playwright evidence), confirming the auto-map at depth 1.0 is live, not just visually attached. | Agent drops the operator and screenshots a single static frame, declaring 'mapping applied' without pressing Play across multiple frames -- this is exactly the stateful-modulation trap (Rule 2/learning #44): a single frame cannot show LFO/Kentaro modulation. |
| X267-2 | #267 | In Operator Rack, confirm 'Sidechain', 'Gate' (GATING), and 'MIDI Envelope Stutter' operator types are addable/available (no greyed-out or 'available:false' disabled state) -- attempt to add each of the three via the browser op-tab drag-to-track-header path. | Screenshot showing each of the 3 operator types successfully added to the rack list with normal (non-disabled) styling -- zero of them show a disabled/unavailable visual treatment, matching the PR's claim of flipping availability to true for all three. | If even one of sidechain/gate/midiEnvStutter still renders disabled or fails silently on drop, the PR's 'zero available:false remain' claim is false but would be missed by only testing one of the three types. |


## NEW-rack-macros (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X235-1 ⚠GAP | #235 | Setup: New Project -> add MIDI track -> drag Sample Rack from INSTRUMENTS onto it -> click '+ Add pad' (data-testid rack-add-pad) 8 times, then click '+ Add macro' (data-testid rack-add-macro) in the Macros section (showMacros block, RackDevice.tsx:539-548) 8 times until the button reads '+ Add macro (max 8)' and is disabled. Drive: confirm the UI itself correctly refuses a 9th macro (button disabled + toast 'Macro limit reached (max 8 per rack)'). | Screenshot: Add-macro button disabled at 8, toast text exact-matches 'Macro limit reached (max 8 per rack).' — this part is WIRED and should PASS. | Stopping here and declaring the fan-out cap 'enforced' — the UI-side cap passing does NOT prove the backend enforces it. |


## NEW-rack-pad-delete (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X239-1 ⚠GAP | #239 | Setup: New Project -> add MIDI track -> drag Sample Rack on -> click '+ Add pad' twice (pad A, pad B). Select pad A, load a sample, set a MIDI trigger event for it on the timeline (performance lane). Select pad A again, in the macro editor add a macro route targeting pad A's Opacity param (rack-route-pad control). Then select pad B, load a different sample, leave it with no trigger/macro. Drive: select pad A, click 'Delete pad' (data-testid rack-pad-delete-<id>). | After deletion: pad A disappears from the pad grid (rack-pad-grid), its trigger event no longer fires on playback (no sound/frame change at its former timeline position), and its macro route is gone from the macro's route list. Pad B's trigger event and remaining routes are UNCHANGED (still fire correctly), and no crash/error toast appears. Screenshot before/after pad grid + macro route list + playback of pad B's trigger position. | Deleting a pad and only checking 'the pad is gone from the grid' without separately verifying (a) its trigger events are actually pruned from the performance lane and (b) the surviving pad's data is untouched — a partial cleanup (per the PARTIAL verdict on removeRackPad) could leave orphaned trigger events that silently no-op or crash on later replay. |


## NEW-render-field-eval (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X268-1 | #268 | Throwaway project. Add an automation lane via the Automation toolbar 'Add Automation Lane' picker: set 'Domain:' dropdown (data-testid=lane-domain-select) to the Y option, draw a multi-band curve in the lane editor, apply it to an effect param, then render/export the frame. Then remove the axis binding (set Domain back to T / delete the lane) and export again. | PIL pixel-row-average comparison on the Y-domain export: sample pixel rows across the export and confirm value varies by row into visibly distinct horizontal bands matching the drawn curve (effect-amount-nonzero precheck per Rule 5). Then byte-compare or perceptual-diff the second export (binding removed) against a baseline export taken before ever touching Domain, confirming they are identical -- validating the 'additive/no-op when absent' claim. | Agent renders once with Y-domain and calls it done without ever removing the binding to check the byte-identical-when-absent regression claim, which is the actual regression PR268 guards against. |


## NEW-schema-v3 (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X189-1 | #189 | Setup: copy backend/tests/test_project/fixtures/project-v2-legacy.glitch to a scratch path (e.g. ~/Desktop/legacy-v2-test.glitch) so it is reachable from the native file picker. Drive: in Creatrix, use File > Open Project (hotkey Cmd+O, per default-shortcuts.ts action 'open') and select legacy-v2-test.glitch. | A toast appears with the exact text 'Project file rejected: Unsupported project format (v2 / pre-3.0) — this version can't open it.' (frontend/src/renderer/project-persistence.ts:1849 prefixes 'Project file rejected: ' onto backend/src/project/schema.py:23 V2_UNSUPPORTED_MESSAGE). The app must not crash, must not show any timeline/tracks from the v2 file, and the previously-open project (or Welcome screen) must remain visible/unchanged underneath. | A rubber-stamp pass would eyeball 'some error showed up' — this row requires the LITERAL current message text (which was reworded from the PR's original checkpoint copy 'v2 projects unsupported — start a new project'); if the executor accepts stale/paraphrased text or a silent no-op load, the wording drift and any partial-load regression go undetected. |


## NEW-voice-caps (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X194-1 | #194 | Setup: throwaway project, add a Performance/MIDI track, load a sampler instrument, apply a visibly-parameterized datamosh effect to the track's effect chain (nonzero amount, e.g. mosh intensity slider > 50%). Drive: arm the track (track-header__auto-btn, title 'Arm for automation recording' is unrelated — instead use the pad grid / keyboard to trigger 2 overlapping sampler voices, e.g. hold pad A, then while still held press pad B so both voices are simultaneously active), then press Play/Space to advance playback across at least 30 frames while both voices sound, then release one pad (stealing/ending that voice) while the other continues. | Per rule 2 (stateful effect), scrub/step through >=10 consecutive frames during the two-voice overlap and screenshot each: the datamosh visual state (block corruption pattern) for the surviving voice must NOT visibly reset/restart or flash back to its initial frame at the moment the other voice is released — a reset (sudden clean frame or corruption-pattern jump) is the cross-contamination bug this PR fixes (backend/src/zmq_server.py:1154 _get_composite_states surgical cleanup). Precheck: confirm the datamosh effect amount parameter reads nonzero in the Inspector before judging any 'render looks unaffected' verdict. | A single-frame screenshot at voice-release would show nothing (datamosh is temporal) and get rubber-stamped as pass; the row forces multi-frame comparison across the release boundary, which is the only place the per-voice state bleed would actually be visible. |


## Stage A (2 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X176-2 ⚠GAP | #176 | Throwaway project. Add 3 clips in sequence on one track. Select the first clip and press Shift+Backspace (default-shortcuts.ts binds 'ripple_delete' to shift+backspace). Repeat via right-click context menu 'Ripple Delete' item on another clip. | Screenshot showing later same-track clips shift left by exactly the deleted clip's duration after both the hotkey and the menu-item path; one undo (Cmd+Z) restores original layout in a single step. | Confirms the WIRED half of PR176 (ripple delete) actually works end-to-end via both hotkey and menu, so the row set doesn't blanket-fail the whole PR when only rippleTrimClipOut is broken. |
| X335-2 | #335 | Drive: launch the packaged Electron app (not `npm start` dev mode) via the built app bundle, play back a project's audio track for at least 5 seconds continuously. | Shell check after: `tail -5 ~/.creatrix/audio-bake-log.jsonl` shows the newest record has `"app_mode":"packaged"` (not 'test' or missing/'unknown') and `duration_s >= 5`; running the same check after a dev-mode (`npm start`) launch instead shows `"app_mode":"dev"`. | If resolveAppMode() mis-detects packaged vs dev, bake-gate noise-filtering (scripts/check_bake_gate.py) would either wrongly exclude real production usage or wrongly count dev/test noise toward the bake clock — a check that only confirms a log line exists (not its app_mode value) would miss the misclassification. |


## Stage B (6 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X156-1 | #156 | Continuing from a project with a Sampler added (per X155-1): set Speed to a distinctive non-default value (e.g. 3.5), Opacity to 0.35, Blend to 'screen', and Start to 10. Save the project (File > Save / Cmd+S). Quit and relaunch the app (or File > Open Recent / File > Open Project) and reload the same project file. | After reload, open the Instruments/device-chain SamplerDevice panel for that track and read the input values directly off the UI: sampler-speed input shows 3.5, sampler-opacity slider shows 0.35, sampler-blend select shows 'screen', sampler-start shows 10 — exact round-trip, not defaults. Additionally, the composited preview visually matches the pre-save appearance (multiply/screen darkening or lightening consistent) at the same playhead frame. | Checking only that the project file opens without crashing (back-compat check) while skipping the actual value comparison would rubber-stamp a silent reset-to-defaults on load, which the hydrateStores clamp/validation code could mask as 'valid' while discarding the user's actual settings. |
| X170-1 | #170 | Open an existing project. Use File menu > 'Save As...' (Cmd+Shift+S per menu.ts label 'Save As...\tCmdOrCtrl+Shift+S'). In the native save dialog, save under a new filename. After save completes, make a small edit (move a clip) and press Cmd+S. | Screenshot of the title bar shows the NEW filename after Save As; after the Cmd+S edit-save, use Finder/shell (`ls -la` on the project directory) to confirm a '<newname>.bak.1' backup file now exists containing the pre-edit state. | A CU agent could click Save As, see the dialog, and rubber-stamp success without confirming the title bar actually rebinds AND without checking a .bak.1 file was created on the subsequent manual save — both halves of the claim must be checked. |
| X170-2 | #170 | Repeat manual Cmd+S 6 times in a row on the same project (each time nudging a clip so there's a real diff) to exceed the 5-backup cap. | Shell check (`ls` on project dir) shows exactly 5 .bak.N files (.bak.1 through .bak.5) present, never 6+ — proves the rotation cap holds under repeated saves rather than growing unbounded. | Stopping after 2-3 saves would never exercise the cap boundary; must save past the cap (6 times) to falsify an off-by-one or unbounded-growth bug. |
| X172-1 ⚠GAP | #172 | GAP ROW (expected to fail today). Throwaway project: add a video clip referencing a file, then move/rename that file on disk outside the app, then reopen the project. The 'Media Files Missing' dialog (aria-labelledby 'relink-dialog-title', text 'Media Files Missing') appears; click 'Skip' on the missing entry to dismiss it as flagged missing. | Screenshot the timeline: the VIDEO clip shows NO visible 'missing' indicator/badge (unlike the AUDIO path, which renders class 'audio-clip--missing' + red 'MISSING' label per AudioClipView.tsx) — confirms the PR's own PARTIAL/gap note that only audio clips got the badge. | The gap is that a CU agent could rubber-stamp 'relink dialog works, feature done' after seeing the dialog and Skip button function correctly, without checking the timeline clip itself afterward — the row must explicitly look for and NOT find a missing-badge on the video clip to expose the still-open half of the claim. |
| X305-2 | #305 | Setup: add a Granulator instrument to a track and set its render mode toggle (if exposed in GranulatorDevice.tsx UI) to GPU. Save the project (Cmd/Ctrl+S), close, and reload the project. | After reload, the Granulator's render mode control still reads GPU (not silently reset to CPU) — confirms render_path:'gpu' round-trips through save/load and is included in buildGranulatorLayer's emitted payload. | If render_path were not serialized, the reload would silently reset every granulator to the CPU render arm with no error toast — a check that only reopens the project without inspecting the render-mode control would miss the silent downgrade. |
| X365-1 | #365 | Throwaway project. Open MIDI Map mode ('MAP' toggle), learn/bind at least 2 CC-to-bank-slot mappings on a connected virtual/physical MIDI controller. Close the app fully (quit Electron, not just the window) or physically disconnect+reconnect the controller device. Relaunch the app / reconnect and reopen MIDI Map mode; screenshot the midi-map-grid slot states. | The same 2 CC slots must show as already-bound (populated CC# + target badge) in the grid with no re-learning needed, matching the pre-relaunch screenshot exactly. If a DIFFERENT/unknown controller identity is used instead, the grid must show all slots empty (no bindings applied) -- second screenshot proving negative case. | A rubber-stamp pass would only check the known-controller restore and skip the negative case, missing a bug where fingerprinting is too loose and bindings leak across different controllers. |


## Stage C (11 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X162-1 | #162 | Throwaway project. Add a video clip to a track, open the effects browser and type 'spectral' into the search box (placeholder 'Search...', data-testid 'browser-search-input'). Confirm the 6 results (spectral_shift, spectral_comb, spectral_smear, spectral_formant, spectral_parity, spectral_inversion) appear, then drag 'spectral_shift' onto the clip's device chain. | Screenshot of the search results listing all 6 spectral_* entries; after drag, the EffectRack for that clip shows the spectral_shift device (no longer showing the 'No effects. Add from browser.' placeholder). | A stale-cache or lazy-loaded effect list could show fewer than 6 entries after a fresh app launch; must literally count 6 results, not assume the PR's claim. |
| X162-2 | #162 ▶multi-frame | With spectral_shift added to the clip's chain (from X162-1), turn its shift-amount knob to a clearly nonzero value, then press Play and observe the preview across at least 3 frames. | Screenshots at 3 distinct playback timestamps show visibly different frame content vs. the unprocessed clip (spectral warping artifacts change frame-to-frame) — precheck: confirm the knob value shown in the UI is nonzero before judging the render broken/working (P1-A lesson). | Judging a spectral effect from a single paused frame could miss that it needs Play + multiple frames to show frequency-domain warping, or could false-fail if the effect param defaulted to 0/off. |
| X166-1 | #166 ▶multi-frame | Throwaway project. Add a video clip, apply the reaction_mosh effect from the effect browser with default params (do not touch pde_steps_per_frame), then press Play at 1080p export resolution setting and observe playback smoothness across the clip's full duration. | Screenshot/observe: no stutter/dropped-frame indicator and preview keeps pace with the transport playhead; optionally export a 5s section and confirm export time stays proportionate (not the ~3x slower budget blowout the old default-3-steps caused) — precheck the pde_steps_per_frame field in the effect's param panel reads 1 (not 3) before judging performance. | Testing with a manually-set pde_steps_per_frame=3 (old default) would falsely confirm/deny the fix; must verify the displayed default value is 1 before timing playback. |
| X176-1 ⚠GAP | #176 | Throwaway project. Add a clip on a track with at least one more clip later on the same track. Select the earlier clip, drag its RIGHT trim handle (`.clip__trim-handle--right`, the 8px grip-bar strip at the clip's right edge) leftward to shorten it. Then right-click the clip and inspect the context menu — note it offers 'Split at Playhead', 'Duplicate', 'Delete', 'Ripple Delete', 'Rename', 'Color', 'Speed/Duration...', 'Reverse' but NO ripple-trim entry, and there is no toolbar/modifier-key affordance for ripple-trim anywhere in the app. | Screenshot of context menu items list (must show no ripple-trim item) + screenshot before/after the manual right-edge trim showing the downstream clip's start position UNCHANGED (a gap opens between the trimmed clip and the next clip) — proves rippleTrimClipOut cannot be triggered from the UI even though the store method exists. | A CU agent could rubber-stamp this by only testing plain trim (which works) and never checking whether downstream clips ripple-shift, missing that the advertised ripple-trim behavior (PR claims 'shortens clip and shifts downstream clips') is completely unreachable from the UI. |
| X180-1 | #180 | Place 3 clips on a track with empty gaps between them. On the empty track background (not on a clip), press-drag a rectangle (marquee) that overlaps 2 of the 3 clips, then release. | Screenshot showing exactly the 2 overlapped clips highlighted/selected (selection ring or highlight state) and the 3rd clip NOT selected. | Skips verifying the overlay actually commits a selection (vs. just rendering a visual drag-rect with no effect) — the checkpoint requires clips to visibly become selected after release, not just during the drag. |
| X180-2 | #180 | Start a marquee drag over empty track background covering 2 clips, then press Escape BEFORE releasing the mouse button. Separately, click-and-drag starting directly on top of a clip's body (not empty background). | Screenshot after Escape-cancel showing selection state unchanged from before the drag started; screenshot after drag-on-clip-body showing the clip moved/dragged (not a marquee rectangle drawn). | Without the Escape-mid-drag check, a marquee that always commits regardless of cancel key would falsely pass; without the drag-on-clip check, an overlay that steals events from clip-move would falsely pass too. |
| X181-1 | #181 | Double-click a clip's label/name text on the timeline to enter rename mode, type a new name (e.g. 'MyRenamedClip'), press Enter to commit. Save the project (Cmd+S), fully quit the app, relaunch, and reload the project. | Screenshot of clip label reading 'MyRenamedClip' immediately after Enter, and the same label still reading 'MyRenamedClip' after quit+relaunch+reload — proves renameClip persists through the save/load round-trip, not just in-memory. | A rename that only updates the in-memory label (never serialized) would look identical in-session but revert silently after reload — this is the exact class of bug Stage B exists to catch. |
| X181-2 | #181 | Right-click a clip to open its context menu, hover the 'Color' row to reveal its swatch-dot row (8 swatches, `.context-menu__swatch`), click one swatch (e.g. a distinct red or blue). Then save, quit fully, relaunch, reload. | Screenshot of the clip body's tint changing to match the clicked swatch color immediately after click, and the same tint present after reload — pixel-sample the clip background color in both screenshots and confirm they match (not default color). | Same persistence trap as X181-1 — swatch click could tint the clip visually via ephemeral component state while never calling the persisted store setter, passing an in-session check but failing on reload. |
| X305-1 | #305 | Setup: create a new MIDI track ('+ MIDI Track'), open INSTRUMENTS tab, drag 'Granulator' entry onto the track (per InstrumentsBrowser.tsx label 'Granulator', id 'granulator') — a base video clip is required first per the browser's own hint text. Drive: once the Granulator device (GranulatorDevice.tsx) appears and is configured, right-click the track header and select 'Delete Track' from the context menu (Track.tsx label 'Delete Track', hotkey shown via delete_track shortcut). | After delete, run `window.__INSTRUMENTS_STORE__?.getState()` (or the store devtools) and confirm no frameBank/granulator entry keyed by the deleted track id remains — zero orphaned entries for that id. | Before the fix, removeTrack left a leaked granulator + frameBank entry referencing the now-nonexistent track id — a check that only confirms the track visually disappears from the timeline would miss the store-level leak. |
| X363-1 | #363 | Throwaway project. Add fx.cellular_pixel_sort (search 'Cellular Pixel Sort' in Effects Browser search box, add to a clip with natural non-noise footage) at default params (do NOT touch ca_rule). Press Play and let it run 20+ frames, screenshotting the preview canvas at frame 1, frame 10, and frame 20. | Per-pixel diff between frame1-vs-frame10 and frame10-vs-frame20 screenshots must stay visibly above a near-zero threshold at all three checkpoints (image keeps changing) rather than the frame-10/frame-20 pair looking identical (decayed to static) -- this is the ca_rule='daynight' default fix under test. | Known trap (learning #44): a CU agent that screenshots only frame 1 will see visible motion and rubber-stamp pass even if the effect decays to a frozen frame by frame 15 under the old 'life' default. |
| X370-1 | #370 | Throwaway project. Add fx.transition_column_cascade (search 'Column Cascade' in Effects Browser) to the tail of a clip. Using the effect's frame_index/progress parameter slider (or scrub the playhead across the clip's last portion so frame_index sweeps 0..duration_frames), screenshot the preview at frame_index=0, 25%, 50%, 75%, 100% of duration_frames. | Columns must progressively reveal left-to-right (or per documented direction) across the 5 screenshots, reaching a FULLY revealed frame (matching the untouched source frame, frame_a=black stand-in fully gone) at frame_index=duration_frames. Repeat check for transition_column_cascade_reverse (columns reveal opposite direction) and transition_row_waterfall (rows instead of columns). | PR self-declares this is a single-frame stand-in (frame_a=black), NOT true two-layer A/B compositing -- a tester expecting an actual second clip revealed underneath would wrongly fail it; the oracle here is scoped to the documented black-to-source reveal contract, not real layer transitions. |


## Stage C — variant (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X255-1 ⚠GAP | #255 | Throwaway project. Add a Rack Device to a track (Instrument Browser -> Rack). Load a pad with a sound. Press Play so the pad is voicing live, then click the freeze toggle button labeled '❄ Freeze' (data-testid=rack-freeze-toggle) in the rack breadcrumb while voices are still sounding. Watch the button label change to '… Freezing' then to '❄ Unfreeze — restore live voices'. After it reads Unfreeze, press Play/scrub across multiple frames of the track and LISTEN/WATCH whether the previously-live pad now plays back as a baked/frozen clip (audibly identical loop, no live-triggered variation) versus still just being silent or unchanged pre-freeze audio. | Screenshot of button in all 3 states (Freeze/Freezing/Unfreeze) proves the FSM transitions (WIRED per code). Then: play across >=3 frames after Unfreeze and confirm there is NO audible/visible baked-clip playback distinct from the live pad — per PR body this PR explicitly defers the actual bake-to-clip render-loop consumer to B10.1b, so frozen state should have no observable playback effect. | CU agent sees the button flip to 'Unfreeze' and rubber-stamps 'freeze works' from the label alone, without ever pressing Play across multiple frames to check the PR's own admitted gap (bake is a stub, no render-loop consumer wired). |


## Stage C — variant (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X255-2 ⚠GAP | #255 | Throwaway project, Rack Device with 2 pads. Trigger pad A voicing (play/hold), then rapid-fire trigger pad B and click '❄ Freeze' on the track twice in a row before the first freeze completes (button is disabled mid-freeze per aria-disabled on freezeFsm==='freezing'). | Screenshot showing the Freeze button is disabled (grey/unclickable) while label reads '… Freezing' -- confirms double-bake guard from PR claim (queue sorted by frameIndex, MAX_FREEZE_QUEUE cap). No crash, no duplicate freeze toast/log entry. | Agent double-clicks fast and if a second freeze silently fires (no disabled state), that's the double-bake guard failing -- easy to miss if only single-clicking slowly. |


## Stage C — variant (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X266-1 | #266 | Throwaway project. Add an effect that is field-capable (per DeviceCard's 'Field…' dropdown option, e.g. a color/transform effect). On the device chain card, open the param's field-assign control and select 'Field…' from the dropdown, then pick an imported image as the field source. Render/scrub the frame. | Export the frame and open in PIL: sample pixel values across the frame at several (x,y) coordinates and confirm the effect's applied strength/color varies per-pixel correlating with the image's luma at those coordinates (not a single flat scalar value everywhere) -- per Rule 3, this must be checked via export+PIL since alpha/field intensity differences may be subtle in JPEG preview. | Agent eyeballs the live preview and declares 'looks textured' without an actual PIL pixel-value diff across multiple sampled coordinates, missing a case where the field silently collapsed to its mean (flat) value. |


## Stage E (8 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X183-1 | #183 | Tab (keyboard only, no mouse) through the app until focus lands on any Knob control (e.g. a device parameter knob) — the focusable element is an SVG with role=slider. Note the current displayed value, then use Arrow keys to change it. | Since accessibility tree text isn't visually screenshottable, use a DOM/accessibility inspection: run the app in dev tools and confirm the focused element has attributes role="slider", aria-valuemin, aria-valuemax, aria-valuenow matching the displayed value, aria-orientation="vertical" (for Knob) — screenshot the DevTools Elements/Accessibility panel showing these attributes on the focused node. | Purely visual eyeballing of the knob turning would miss whether aria-valuenow actually updates in the DOM as the value changes (vs. only the SVG rotation visual updating) — a screen-reader user needs the attribute, not just the pixels. |
| X183-2 | #183 | Right-click a clip to open its context menu (role=menu). Using ONLY the keyboard (no mouse), press ArrowDown repeatedly to move focus through menu items, then Home to jump to the first item, End to jump to the last item, and Enter to activate the focused item (e.g. 'Duplicate'). | Screenshot sequence showing the focus ring/highlight moving to a different menu item on each ArrowDown press, jumping to the top on Home and bottom on End, and the corresponding action actually firing (e.g. a duplicate clip appears) after Enter — proves roving-tabIndex keyboard nav is real, not just declared. | role=menuitem attributes existing in markup doesn't guarantee the ArrowDown/Home/End keydown handler is wired — this row exercises the actual keyboard interaction rather than inspecting static ARIA attributes. |
| X184-1 | #184 | Using Tab only (no mouse), cycle through every interactive control on one full panel (e.g. a device chain card's toggle, sliders, knobs, and the track header's mute/solo/lock buttons) and stop on each one. | Screenshot at each Tab stop showing a visibly distinct focus ring (the var(--cx-focus-ring) outline) around the currently focused control — zoom crop each screenshot on the focused element to confirm the ring pixels are present and not transparent/invisible. | A CSS variable being defined (var(--cx-focus-ring)) doesn't guarantee it resolves to a visible, non-transparent color at runtime in the current theme — this row requires seeing the literal ring pixel, not just reading the stylesheet source. |
| X186-1 | #186 | Using keyboard only, open Preferences (app menu or its hotkey) and press Tab 20+ times while watching where focus lands; confirm it stays inside the dialog (cycles back to the first control) rather than escaping to the background app or menu bar. Then press Escape to close it. Repeat for at least 2 more of: About, Export, Preset Save, Feedback, Telemetry Consent, Relink, Crash Recovery, Unsaved Changes, Speed. | Screenshot sequence showing focus ring never lands on a background-app element across 20 Tab presses (each screenshot shows the ring inside the dialog boundary), and screenshot showing the dialog closed after Escape with no residual overlay. | Testing Tab only 2-3 times could miss a focus trap that leaks after wrapping around once — the PR's own test claims '20 Tab presses' specifically because early-cycle leaks are the common failure mode; this row must match that exact repetition count to be a faithful check. |
| X186-2 | #186 | Open Preferences, navigate to the ShortcutEditor's key-capture UI, click into a shortcut's rebind field to enter capture mode, then press Escape while capture mode is active. | Screenshot showing the Preferences dialog STILL OPEN after Escape (capture mode consumed the Escape to cancel the key-capture, not to close the dialog) — contrast with a second screenshot where Escape pressed OUTSIDE capture mode does close Preferences. | A naive Escape-always-closes-dialog implementation would incorrectly close Preferences mid-rebind, silently discarding the in-progress capture without the user noticing — this row isolates that exact interaction claimed as 'UNVERIFIED' in the PR evidence. |
| X188-1 | #188 | Open the Automation panel and zoom in on an automation lane with at least one node. Click 3-4px to the SIDE of the visible node glyph (not dead-center on the small dot) and drag it vertically. | Screenshot before/after showing the node's value actually changed (curve shape updated) even though the click/drag started slightly off the visual glyph center — proves the r=12 invisible hit-ring (vs r=4-6 visual glyph) is functional. | Clicking dead-center on the node (the easy, obvious spot) would pass regardless of whether the hit-ring exists — the row must deliberately click near the edge of the enlarged hit area to actually exercise the PR's claim. |
| X188-2 | #188 | On a clip with a visible right trim handle, click 2px inside the clip body from the right edge (near but not exactly on the 8px `.clip__trim-handle--right` strip) and drag left to trim. Also click 2-4px off-center on a device chain card's bypass toggle (`.device-card__toggle`) and on a track header mute/solo/lock button (`.track-header__btn`). | Screenshot before/after each interaction showing: the clip duration shortened (trim registered), the device toggle visibly flipped enabled/disabled state, and the mute/solo/lock button visibly toggled active state — despite none of the clicks landing dead-center. | Testing only dead-center clicks (the default CU click target) never exercises whether the hit area was actually enlarged versus just visually widened; a click 1px off from a target that only LOOKS bigger but isn't functionally bigger would silently miss, and this row is designed to catch exactly that. |
| X347-1 | #347 | Open the Effects Browser panel and click its 'tool' tab (data-testid=tool-tab-content). Screenshot the 'Cursor Tools' folder list and the 'Mask Tools' folder list below it. Click the 'Ripple Delete' tool row, then click a different tool row (e.g. 'Trim'). | Each row in both TOOL_ENTRIES and MASK_TOOL_ENTRIES lists (data-testid=tool-item-<id>) must show an SVG icon span (data-testid=tool-icon-<id>) left of its text label, rendered in Block style (square-cap strokes, currentColor -- visually a thin geometric glyph, not a placeholder box or missing image). The currently active tool's row must show the '●' active badge (effect-browser__tool-active-badge) and be visually distinguished from inactive rows; clicking a second tool moves the badge to the new row and removes it from the old one. | PR itself says icons shipped 'not mounted anywhere' at merge -- later PR wired them into EffectBrowser; a lazy tester could read only the original PR body and mark this cuTestable=false or skip verifying the icons actually render pixels (vs. just DOM nodes existing). |


## Stage F.1 (1 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X329-1 ⚠GAP | #329 | Setup: in the masking workflow, generate an AI subject matte (mask_ai_generate) on a node so a sidecar file is written to disk under ~/.creatrix (per MASKING-INTERACTIONS flow). Confirm via Finder/shell the sidecar file exists. Drive: in MaskStackPanel, click the node's delete button (aria-label 'Delete node', mask-stack-panel__delete-btn). | Shell check: `ls ~/.creatrix/**/*<node-id>*` (or equivalent sidecar glob) BEFORE delete shows the file, and the SAME command AFTER delete shows it gone. If the file still exists after delete, that proves mask_gc_sidecars has no real caller (allowlist-only, per grep showing 0 renderer call sites). | GAP: grep -rn mask_gc_sidecars frontend/src/renderer returns 0 matches — the IPC command is allowlisted but nothing invokes it on node delete, so this row is EXPECTED TO FAIL today (sidecar file persists after delete); a rubber-stamp pass that only checks the allowlist entry exists would wrongly mark this WIRED. |


## Stage H (2 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X332-1 | #332 | Setup: build an effects chain node that references a video/image field source, then delete/rename the underlying source file on disk so it becomes 'dead' while the project keeps referencing it. Drive: press Play and let the timeline render at least 3-5 seconds (180+ frames at 60fps) through the dead-source node, then stop, restore the source file, and press Play again for another few seconds. | Backend log (or in-app console/log viewer if present) shows exactly ONE dead-source warning line for the entire first playback run (not one per frame), and exactly one NEW warning appears if the source dies again after being restored and re-killed — confirms _warn_once/_clear_warned dedup and reset behavior. | Rule 2 trap: verifying with a single paused frame would show nothing meaningful for this per-frame-vs-once dedup claim (a single frame can't distinguish 'once' from 'every frame') — must play across many frames to see the counter NOT incrementing. |
| X345-1 | #345 | Fresh project, throwaway. Add a rack pad (Instruments panel) on Track A and an effect (Effects Browser -> add any effect) on Track B; make Track B the armed/active track (click its track header to select it). Click the rack pad on Track A. Read the statusbar chip to the left of the 'MAP' toggle button (data-testid=statusbar-map-toggle) at bottom of window. Then click the effect row on Track B's effect chain and re-read the chip. | After clicking Track A's pad while B is active, chip text must NOT read 'pad · Track A' (it must show Track B's own focus or vanish) -- screenshot #1. After selecting Track B's effect, chip text updates to 'effect · <effect name>' -- screenshot #2. Then click empty canvas/deselect everything and confirm the chip DOM node is absent entirely (kind==='none' renders null). | Rubber-stamp risk: agent sees ANY chip text and calls it pass without checking it didn't steal the non-active track's selection, and without confirming full disappearance on deselect. |


## Stage I (6 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X157-1 | #157 | Throwaway project. Add a video track, select it, add an effect from the fx browser (search 'spectral' box placeholder 'Search...'), then in the Automation panel toolbar click Mode picker and add one lane with Mode=Drawn (smooth) and a second lane with Mode=Operator/Trigger set to Gate. In the timeline, look at each lane's SVG rendering. | Screenshot: the gate/trigger lane renders red trigger blocks (class 'auto-trigger-block', fill=lane color at 0.5 opacity per AutomationLane.tsx render branch) while the smooth lane renders a continuous curve, not blocks — visually distinguishable badge/render difference between the two lanes. | Rubber-stamp risk: clicking through without actually comparing the two lane render styles side-by-side would miss that isTriggerLane() controls a real visual branch (auto-trigger-block vs curve), not just an internal flag rename. |
| X157-2 | #157 | On the same automation lane set to Mode=Step (via the lane's mode control in AutomationToolbar 'operator' mode with step interpolation), draw two points with different values, then press Play through the region spanning both points. | Playhead value display / parameter meter jumps discretely at the second point's time rather than ramping — screenshot at 3 timestamps between the points shows the SAME value as the left point until the exact jump frame (per automation-evaluate.ts:144-145 'step' returns a.value with no interpolation). | A CU agent glancing at a single frame mid-segment could wrongly conclude interpolation is happening if it doesn't sample multiple times between the two points — must screenshot at 2+ intermediate times to prove flatness. |
| X158-1 | #158 | Throwaway project. Add a video track, arm it (select the track so automation toolbar becomes active), open the Automation param picker, and BEFORE picking a param, set the 'Domain:' select (title tooltip 'Axis the lane's curve reads along...') to 'Y (scanline)'. Then pick any effect param to create the lane. Save the project (Cmd+S), close and reopen it. | After reopening, reselect the same lane/param and confirm the Domain select still shows 'Y (scanline)' (not reset to 'Time') — proves axisBinding.domain='y' persisted through save/reload round-trip. | Testing only the immediate UI state (Domain shows Y right after picking) would rubber-stamp persistence without exercising the actual save/reload round-trip where a schema-field bug would surface. |
| X158-2 | #158 | With the Y-domain lane from X158-1 still selected, draw automation points on it and press Play. Watch the video preview frame for any spatial gradient effect tied to the Y-axis. | Screenshot the preview during playback: NO per-pixel Y-gradient modulation should appear (PR explicitly scopes render support as NOT YET implemented at C2/C3 tier) — this is the expected/passing state, not a bug. | A CU agent unfamiliar with the PR's honest scoping note could misreport 'no visual effect = broken feature'; the row must confirm ABSENCE of render change is correct today, not chase a render bug that doesn't exist yet. |
| X302-1 | #302 | Setup: open MIDI Settings panel (Performance area). Connect/simulate a MIDI controller and right-click a knob to 'learn' at least 3 CC mappings (per empty-state hint 'No CC mappings — right-click a knob to learn'). Drive: rapidly twist the controller to send a burst of CC messages on one mapped knob, and WHILE the burst is still arriving click the 'Clear All' button in the CC Mappings section header (midi-settings__clear-btn). Wait 2s past any flush-timer window, then re-observe the knob and the CC Mappings list. | CC Mappings list shows 0 rows (empty-state text 'No CC mappings — right-click a knob to learn' reappears) and the knob's on-screen value does NOT jump to a stale CC value after the Clear All click — confirms _resetCCLimiter() ran before the mappings were cleared. | Without the timer-cancel fix, a pending throttled CC flush could fire after Clear All and repopulate/apply a stale CC value — a naive check that only looks at the mapping list right after clicking (not after waiting past the flush window) would miss the resurrection. |
| X393-1 ⚠GAP | #393 | Throwaway project. Open an Automation lane with >=2 breakpoint nodes, drag a marquee-select rectangle over both nodes (click-drag on empty lane background, not on a node) to multi-select them (both nodes should show selected styling). With the nodes selected, press the platform copy hotkey (Cmd+C on Mac / Ctrl+C), move the playhead elsewhere on the lane, then press the platform paste hotkey (Cmd+V / Ctrl+V). | Marquee-select and group-move (drag one selected node, confirm both move together, quantize-snapped) must work and screenshot-confirm. Then: pressing Cmd+C/Cmd+V must produce NO new breakpoint nodes at the new playhead position -- screenshot the lane before and after the paste attempt showing zero change, proving copySelectedPoints/pasteAtPlayhead have no bound keyboard shortcut or menu entry reachable by a user. | This is a known GAP (copySelectedPoints defined in store but zero call sites outside tests) -- the row is DESIGNED to fail on the copy/paste step; a tester who only verifies marquee-select+move (which IS wired) and skips the copy/paste half would falsely mark the whole checkpoint green. |


## Stage J (5 rows)

| # | PR | Check (Setup + Drive — literal UI labels) | Oracle (falsifiable evidence) | Trap guarded |
|---|----|----|----|----|
| X152-1 | #152 | Open a project, ensure a Performance (MIDI) track exists (use timeline's '+ MIDI Track' button, Timeline.tsx:199, if none). Add at least one effect from the Effects tab so the chain is non-empty. Open the pad editor for a pad (Performance panel > click a pad to open PadEditor). Under the 'Mappings' section click '+ Add' to create a mod route, pick the just-added effect and one of its parameters from the mapping's selectors. Save the project (Cmd/Ctrl+S or File > Save), then close and reopen it (File > Open Project, pick the same file). | After reload, reopen the same pad's editor: the 'Mappings' section still lists the mod route (not the empty-state text 'No modRoutes — add one to connect this pad to an effect parameter'). Trigger the pad (click/play it) and observe the mapped effect parameter's on-screen slider value change live in the preview/parameter panel, proving the route both persisted and applies. | Verifying persistence by only re-reading the saved JSON's 'modRoutes' key (schema check) without actually triggering the pad afterward to confirm the rename didn't silently break the runtime application path (Pad.mappings vs modRoutes mismatch would show up only at trigger-time, not at save-time). |
| X175-1 | #175 | Throwaway project. Add a performance track and select it (arms it per 'Pads are armed whenever the selected track is a performance track'). Select a clip on that track so the TransformPanel with bounding-box handles appears, enter perform mode, then press Escape once. | Screenshot immediately after the first Escape: the clip selection and TransformPanel/handles are gone, but pad audio/visuals are NOT silenced (panicAll did not fire) — confirms clearSelection() short-circuited before panic per App.tsx:975-984. | Pressing Escape only once without first confirming a clip was actually selected (TransformPanel visible) would rubber-stamp a no-op as a pass; must screenshot the 'before' state showing selection+handles to prove there was something to clear. |
| X175-2 | #175 | Continuing from X175-1 (selection already cleared, no clip selected), trigger a couple of pads to produce sound, then press Escape a second time. | Screenshot/log: this second Escape triggers panicAll (all active pads/sound stop immediately) since selectedClipIds is now empty — proves the fallback panic path still fires when there is nothing to clear. | Testing only the first Escape (selection-clear path) and never the second (panic path) would leave the regression's OTHER half unverified — F-0514-5 required both behaviors to coexist without one breaking the other. |
| X185-1 ⚠GAP | #185 | Add a track with an effect, open the Automation panel/toolbar's 'add lane' param dropdown for an armed track, select 'Mixer' / 'BPM' (the PROJECT_PARAM_OPTIONS entry with effectName='Mixer', paramLabel='BPM'), draw an automation curve on that lane that ramps the value up, then press Play and watch the transport BPM readout while comparing it to the project's saved bpm. | Since applyProjectModulations() has zero call sites in production code (grep -rn "applyProjectModulations(" frontend/src/renderer confirms it is only referenced inside its own definition file and test files), the transport BPM display should NOT change during playback despite the automation lane being drawn and active — screenshot the transport BPM readout before Play and during Play at a point where the lane value is clearly non-baseline; if the numbers are identical, that confirms the gap. Also grep the saved project JSON's 'bpm' field to confirm it never mutates. | The trap is verifying only that the persisted bpm field stays constant (which it correctly does since P2.1 split bpm/effectiveBpm) and concluding 'PASS' without also checking that effectiveBpm actually moves during playback — since applyProjectModulations is unwired, the modulation has NO audible/visible effect at all, which is the actual defect, not merely 'persisted bpm is protected'. |
| X308-1 | #308 | Setup: in the modulation/effect chain UI, create two operators A and B where B's parameter has NO 'sources' modulation link back to A, but A and B ARE connected only via a plain 'mapping' (e.g. an output routing/mapping, not a '+ Mod' relative lane). Drive: open the routing/cycle-check UI (wherever cycle warnings surface, e.g. modulation graph panel) and trigger evaluation-order computation (e.g. by adding a third operator or saving). | No false cycle-warning toast/badge appears, and the evaluation order in the graph panel is NOT reordered based on the mapping-only edge — confirms graph_adapter.py only builds edges from parameters.sources[].operator_id, not from mappings. | Pre-fix, a mapping-only edge falsely appeared in the cycle graph and could trigger a spurious 'cycle detected' warning or wrong evaluation order even with no real modulation-source link — a check that never builds a mapping-only (non-source) edge would never exercise the fixed code path. |


## Backend-only oracles (not CU-drivable — run as shell commands, 14 rows)

### X149-1 — PR #149
- **Run:** cd backend && python3 -m pytest tests/test_video/test_codec_timeout.py -v; separately construct a truncated/corrupt .mp4 (e.g. `head -c 2048 <any_real.mp4> > /tmp/truncated.mp4`) and attempt to import it via the sidecar's ingest path (or call codec_timeout.av_open_timeout() directly against it in a short python -c script) and time the call.
- **Pass iff:** pytest run shows all test_codec_timeout.py cases passing (exit code 0); the manual truncated-file open either raises CodecTimeoutError or returns a decode error within roughly 5 seconds per frame (wall-clock timed with `time python3 -c ...`), not an indefinite hang (>30s with no return).
- **Trap:** Declaring the timeout 'wired' from the presence of codec_timeout.py alone (static-analysis rubber stamp) without actually feeding it a file that would hang PyAV and timing the real wall-clock behavior.

### X151-1 — PR #151
- **Run:** cd backend && python3 -m pytest tests/test_inj3_composite_caps.py -v (exercises the validator + handler). Optionally drive the live sidecar: send a render_composite ZMQ request with 51 layers, and a second request with one layer's frame_index=-1, using a small python zmq client script against the running dev sidecar.
- **Pass iff:** pytest exits 0 covering both the >50-layer rejection and the negative frame_index rejection; if driving the live sidecar directly, the >50-layer request returns {ok:false, error: mentions 'MAX_COMPOSITE_LAYERS' or layer count} and the frame_index=-1 request returns {ok:false, error:'layer frame_index must be non-negative'} — sidecar process must still be alive and responsive to a follow-up normal render_composite call afterward (no crash/OOM).
- **Trap:** Confirming only that the ok:false error string appears while the sidecar silently died/hung afterward — the no-crash/no-OOM follow-up check is the part a rushed pass would skip.

### X153-1 — PR #153
- **Run:** N/A for direct CU testing — PR #153 explicitly ships computeSamplerVoice.ts/instruments store with SamplerDevice UI deferred (not mounted until PR #155). Verify via: cd frontend && npx vitest run computeSamplerVoice (or the relevant test path) to confirm unit coverage of speed clamp [-8,8]/freeze/reverse/skip exists and passes.
- **Pass iff:** vitest run exits 0 with computeSamplerVoice test file passing; no UI surface exists yet to screenshot for this PR in isolation (superseded by X155 below which is the CU-testable mounted version).
- **Trap:** Attempting to CU-test the Sampler UI against this PR alone and reporting 'can't find sampler controls' as a failure of #153 — the deferred-UI is documented and expected; the real UAT gap is at #155.

### X163-1 — PR #163
- **Run:** grep -rn 'gpu_resources\|GlobalPoolRegistry\|TextureWrapper' backend/src --include='*.py' | grep -v 'backend/src/safety/gpu_resources.py' | grep -v test
- **Pass iff:** Command output is EMPTY (no consumer imports the finalizer module outside its own file and tests) — this is the falsifying evidence that SG-1 ships unused/unwired.
- **Trap:** A rubber-stamp pass would see the file exists (15997 bytes, real code) and mark WIRED without checking for zero consumers; the gap is specifically that nothing calls into it yet.

### X213-1 — PR #213
- **Run:** Shell: `EXPERIMENTAL_AUDIO_TRACKS=true bash scripts/launch-bake.sh` (or run the equivalent `npm start` with the env var set) to launch the app, play back an audio-tracks-enabled timeline for >=60 seconds via the UI, then quit. Then run: `cat ~/.creatrix/audio-bake-log.jsonl | tail -1` to inspect the newest line, and `python scripts/check_bake_gate.py --log ~/.creatrix/audio-bake-log.jsonl`.
- **Pass iff:** The tail -1 JSONL line must be valid JSON containing `flag_on: true`, `callback_errors: 0`, and a `duration_s` >= ~60. Running check_bake_gate.py on day one (fewer than 7 distinct dates logged) must print a FAIL message containing the specific 'no real usage yet' or 'under 7 days'-class reason (per scripts/check_bake_gate.py's documented distinct-failure-message contract) with exit code 1 — not a generic/ambiguous failure.
- **Trap:** A shallow check would only confirm 'a line got appended' and skip verifying the exit-code/message CONTRACT (which failure reason string fires) — that contract is precisely what's needed later to trust the 7-day PASS signal instead of a hand-wavy '7 days probably passed' judgment call.

### X227-2 ⚠GAP — PR #227
- **Run:** grep -rn 'mask_gc_sidecars' frontend/src/ --include=*.ts --include=*.tsx | grep -v __tests__  (confirm zero call sites outside the IPC allowlist in zmq-relay.ts). Then in the running app: wand-sample a matte to create a baked bitmap sidecar PNG (check ~/.creatrix/mask-bitmaps/ for the new file via `ls -la ~/.creatrix/mask-bitmaps/`), delete that matte node via the MaskStackPanel Delete icon (data-testid delete-node), then re-run `ls -la ~/.creatrix/mask-bitmaps/`.
- **Pass iff:** The PNG sidecar file created by the wand sample is STILL present in ~/.creatrix/mask-bitmaps/ after node deletion (file count unchanged / same mtime file exists) — proves gc_orphan_sidecars is never invoked from the delete path, confirming the GAP.
- **Trap:** Verifying only the backend IPC handler exists (via a manual zmq test) and not checking whether the frontend delete flow actually calls it, which would rubber-stamp a dead code path as 'working'.

### X235-2 ⚠GAP — PR #235
- **Run:** With the app closed, hand-edit the saved project's JSON (Save the project from X235-1 first) to give the rack 10 macros (exceeding MAX_MACROS_PER_RACK=8) directly in the file, bypassing the in-app Add-macro button. Then: `grep -rn 'MAX_MACROS_PER_RACK\|validate_rack_macros' backend/src/security.py backend/src/` to confirm these symbols do not exist server-side (per PR claims, zero hits expected). Relaunch the app and File->Open the hand-edited project.
- **Pass iff:** Backend grep returns zero hits for MAX_MACROS_PER_RACK/validate_rack_macros/resolve_rack_macros in backend/ (confirms MISSING at the trust boundary). On load, the app accepts all 10 macros without rejection/clamping/toast, and if any macro routes are mapped to modulation targets, the render/export proceeds using the full 10-macro graph unclamped — proving the backend/render layer performs no server-side cap enforcement, only the disabled UI button did.
- **Trap:** Testing only via the in-app Add-macro button (which correctly refuses #9) and never attempting the JSON-bypass path, which would rubber-stamp a client-side-only cap as a real security boundary.

### X265-1 — PR #265
- **Run:** cd backend && python3 -c "from src.effects.field_params import parse_param_value; from src.effects.field_top25 import is_field_capable; print(is_field_capable('some_non_top25_effect','some_param'))" -- then attempt to invoke the pipeline apply_chain path with a __field__ value on a param NOT in FIELD_TOP25 and confirm a ValueError is raised naming the effect/param.
- **Pass iff:** stdout/stderr shows a ValueError whose message names the effect id and param name, and references gen_field_top25.py --check (per PR checkpoint claim).
- **Trap:** gen_field_top25.py --check is not wired into any CI workflow (grep confirmed no matches in .github/workflows/) -- if this row is skipped, list drift regenerated by hand could silently diverge from the live registry with no automated catch.

### X270-1 ⚠GAP — PR #270
- **Run:** grep -rn lint_gpu_patterns .github/workflows/ ; then run: python3 backend/scripts/lint_gpu_patterns.py against backend/src and check exit code.
- **Pass iff:** grep returns zero matches (confirms UNWIRED claim -- script exists but is not in any CI job today). Running the script manually should exit 0 clean on current tree (per PR's 'clean on current tree' claim) but its absence from CI means a future raw mlx.core allocation outside mlx_resources.py would NOT fail any build.
- **Trap:** Someone assumes 'script exists and passes locally' is equivalent to 'CI is protected' -- the gap this row exposes is that lint_gpu_patterns.py is dead weight until wired into a workflow; that GAP_UNWIRED status must be reported as still open today.

### X273-1 — PR #273
- **Run:** Not CU-testable: this is a pixel-exact CPU-vs-GPU parity check requiring toggling GPU/CPU field codegen at the process level, which is not exposed as a UI control (dispatch is automatic based on Metal availability). Instead run: `cd backend && python -m pytest tests/test_effects/test_field_codegen.py -v` (or equivalent parity test if named differently — grep first: `grep -rl 'field_codegen\|apply_field_pointwise' backend/tests`) and separately force a render on a Metal-capable Mac vs a forced-CPU env var if one exists (`grep -n 'FORCE_CPU\|METAL' backend/src/engine/pipeline.py`), diff output frames with PIL `ImageChops.difference` and assert `max(diff.getextrema())==0`.
- **Pass iff:** pytest exit code 0 AND (if a CPU-forced flag exists) PIL max_abs_diff==0 between the two rendered PNG frames for the same project/frame with a field assigned to a top-25 pointwise param
- **Trap:** rubber-stamping 'GPU codegen works' from reading pipeline.py imports alone, without ever rendering a frame and diffing pixels — PARTIAL pool-teardown claim (release_all_instance_pools) also never independently re-confirmed, so a false 'fully wired' verdict is easy here

### X304-1 — PR #304
- **Run:** Shell: with backend running, inject a NaN frame into a live effect path (e.g. via a debug/test hook or a known effect misconfiguration that produces NaN), let the pressure monitor's last-good-frame cache store it, then mutate the returned buffer in-place from a second consumer and re-fetch the last-good frame.
- **Pass iff:** `grep -n 'output.copy()' backend/src/zmq_server.py` shows the copy call is present (line 885) AND a script that fetches _last_good_frames twice with an in-place mutation between calls shows the second fetch is unaffected by the mutation (values differ from mutated buffer).
- **Trap:** If the copy() were reverted to a bare reference, the second fetch would reflect the external mutation instead of the frozen last-good snapshot — a check that only fetches once would never observe the aliasing bug.

### X304-2 — PR #304
- **Run:** Shell: force the SG-8 pressure monitor thread to hang past its stop() timeout (e.g. patch/monkeypatch the thread's loop to ignore the stop flag in a throwaway debug build) then call monitor.stop() and check backend logs.
- **Pass iff:** Backend log contains a WARNING line matching 'SG-8 monitor thread did not stop within' (backend/src/safety/pressure/monitor.py:95-101) and the app does not crash or attempt a double-stop; `_thread` is NOT nulled when is_alive() is still true.
- **Trap:** Old behavior unconditionally nulled _thread even if the thread was still alive, orphaning it silently with no warning — a check that only confirms 'app didn't crash' without grepping the log would miss the missing-warning regression.

### X333-1 ⚠GAP — PR #333
- **Run:** Shell: `ls backend/tests/test_effects/conftest.py` to check existence, then `cd backend && python -m pytest -n auto tests/test_effects/test_integration.py::test_all_effects_process_without_crash tests/test_effects/test_registry.py -q`.
- **Pass iff:** `ls` shows conftest.py MISSING at backend/tests/test_effects/ (superseded by PR #349's root-level fixture) — the PR's specific claimed file location is a GAP; separately pytest run shows test_registrations_from_prior_tests_do_not_leak (test_registry.py:154-173) PASSES and test_all_effects_process_without_crash shows zero xdist-pollution TypeErrors, confirming the functional intent is covered by #349 even though this PR's exact file claim is stale.
- **Trap:** GAP: the PR claims a directory-local fixture file that does not exist on main; a rubber-stamp check that only runs pytest and sees green would miss that the claimed artifact never shipped as described (superseded, not present) — must explicitly ls for the file before trusting the green suite.

### X335-1 — PR #335
- **Run:** Shell: note current byte size of ~/.creatrix/audio-bake-log.jsonl (`wc -c ~/.creatrix/audio-bake-log.jsonl` or note absent), then run `cd backend && python -m pytest -q` (full suite), then re-check the same file's byte size/mtime.
- **Pass iff:** The real ~/.creatrix/audio-bake-log.jsonl file size and mtime are UNCHANGED before/after the pytest run (isolation fixture redirected CREATRIX_BAKE_LOG to a tmp path) — any new bytes written to the real file is a fail.
- **Trap:** Before this fix, running the test suite polluted the real bake-clock log used for production gating, corrupting the 1-week/2h bake-gate calculation — a check that only confirms tests pass (not that the real log file is untouched) would miss the pollution.

